#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    if (n % 2 == 0)
    {
        cout << "Even " << n;
    }
    else
    {
        cout << "Odd " << n;
    }
    return 0;
}

// time complexity - O(1)
// space complexity - O(1) - একটাই operator হইতাসে
